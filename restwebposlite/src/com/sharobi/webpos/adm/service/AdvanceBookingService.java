package com.sharobi.webpos.adm.service;
import org.springframework.stereotype.Service;
@Service
public class AdvanceBookingService {
	/*private final static Logger logger=LogManager.getLogger(AdvanceBookingService.class);
	private final static Gson gson=new Gson();*/
	
}
